# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
sse-starlette==2.1.3

# --- Config & validation ---
# NOTE: pinned to a minimum, not exact — google-genai 2.11.0 requires
# pydantic>=2.12.5, which is newer than this project's original pin.
pydantic>=2.12.5
pydantic-settings>=2.5.2

# --- LLM / Agents ---
# NOTE: langchain-google-genai must stay >=3.1.0 — older versions can't
# handle Gemini 3.x's "thought signature" requirement for multi-turn tool
# calls and will fail with a 400 InvalidArgument error. The top-level
# "langchain" umbrella package is intentionally NOT listed here — this
# project only imports from langchain_core / langchain_community /
# langgraph / langchain_google_genai directly, and "langchain" pins an
# old langchain-core range that conflicts with the version
# langchain-google-genai 4.x requires.
langchain-core==1.4.9
langchain-community>=0.3.5
langchain-google-genai==4.2.7
google-genai==2.11.0
langgraph>=0.2.45

# --- Vector store & embeddings (fully local, no API cost) ---
chromadb==0.5.18
sentence-transformers==3.2.1

# --- Document parsing (for uploaded router docs/logs) ---
pypdf==5.0.1
unstructured==0.16.0

# --- Database (SQLite by default, swap DATABASE_URL for Postgres) ---
sqlalchemy==2.0.35
aiosqlite==0.20.0

# --- Utilities ---
python-dotenv==1.0.1
